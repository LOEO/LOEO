<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leo.dao.ButtonDao">
    <insert id="insert">
        insert into t_sys_button (name,menuId,class,iconCls,script,orde,createDt,createUser,updateDt,updateUser,enable)
        values (#{name},#{menuId},#{clazz},#{iconCls},#{script},#{orde},#{createDt},#{createUser},#{updateDt},#{updateUser},#{enable})
    </insert>
    <update id="update">
        update t_sys_button set
        <if test="name != null">
            name=#{name},
        </if>
        <if test="menuId != null">
            menuId=#{menuId},
        </if>
        <if test="clazz != null">
            class=#{clazz},
        </if>
        <if test="script != null">
            script=#{script},
        </if>
        <if test="iconCls!=null">
            iconCls=#{iconCls},
        </if>
        <if test="orde!=null">
            orde=#{orde},
        </if>
        <if test="createDt!=null">
            createDt=#{createDt},
        </if>
        <if test="createUser!=null">
            createUser=#{createUser},
        </if>
        <if test="updateDt!=null">
            updateDt=#{updateDt},
        </if>
        <if test="updateUser!=null">
            updateUser=#{updateUser},
        </if>
        <if test="enable!=null">
            enable=#{enable},
        </if>
        id=id
        <where>
            id=#{id}
        </where>
    </update>

    <delete id="deleteById">
        delete from t_sys_button
        <where>id=#{id}</where>
    </delete>

    <select id="list" resultType="com.leo.entity.SysButton">
        select * from t_sys_button
        <where>
            <if test="_parameter != null">
                menuId=#{_parameter}
            </if>
        </where>
    </select>

    <select id="getById" resultType="SysMenu">
        select * from t_sys_button where id=#{id}
    </select>
</mapper>